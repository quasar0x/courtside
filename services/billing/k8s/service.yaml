apiVersion: v1
kind: Service
metadata:
  name: billing
  namespace: courtside
  labels:
    app: billing
spec:
  selector:
    app: billing
  ports:
    - name: http
      port: 8080
      targetPort: http

apiVersion: apps/v1
kind: Deployment
metadata:
  name: clubs
  namespace: courtside
  labels:
    app: clubs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: clubs
  template:
    metadata:
      labels:
        app: clubs
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: clubs
          image: courtside/clubs:0.2.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PORT
              value: "8080"
            - name: MEMBERS_URL
              value: "http://members.courtside:8080"
            - name: PGHOST
              value: "postgres.courtside"
            - name: PGPORT
              value: "5432"
            - name: PGDATABASE
              value: "clubs"
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: postgres
                  key: POSTGRES_USER
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              cpu: 10m
              memory: 24Mi
            limits:
              cpu: 100m
              memory: 96Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]

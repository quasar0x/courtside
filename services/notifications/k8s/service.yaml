apiVersion: v1
kind: Service
metadata:
  name: notifications
  namespace: courtside
  labels:
    app: notifications
spec:
  selector:
    app: notifications
  ports:
    - name: http
      port: 8080
      targetPort: http

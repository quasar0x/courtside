apiVersion: v1
kind: Service
metadata:
  name: members
  namespace: courtside
  labels:
    app: members
spec:
  selector:
    app: members
  ports:
    - name: http
      port: 8080
      targetPort: http

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: eu-residency-isolation
  namespace: residency-eu
spec:
  podSelector: {}
  policyTypes: [Ingress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              region: eu-west
